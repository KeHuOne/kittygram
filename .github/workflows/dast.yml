name: 🔍 DAST - OWASP ZAP Scan

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * 0'  # Еженедельно по воскресеньям в 2:00

jobs:
  zap_scan:
    runs-on: ubuntu-latest

    steps:
      - name: 📡 OWASP ZAP Full Scan
        uses: zaproxy/action-full-scan@v0.10.0
        with:
          target: 'http://185.121.13.20:9000'
          docker_name: 'owasp/zap2docker-stable'
          fail_action: false
          cmd_options: '-a -j'  # активный скан и JSON-отчёт
          report_file: 'zap-report.html'
          json_report_file: 'zap-report.json'

      - name: 💾 Сохранение отчётов как артефактов
        uses: actions/upload-artifact@v4
        with:
          name: zap-dast-reports
          path: |
            zap-report.html
            zap-report.json

  send_message:
    name: 📣 Telegram Notification
    runs-on: ubuntu-latest
    needs: zap_scan

    steps:
      - name: 📬 Отправка отчета в Telegram
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_KITTY_TO }}
          token: ${{ secrets.TELEGRAM_KITTY_TOKEN }}
          message: |
            ✅ OWASP ZAP DAST-сканирование завершено.
            📄 Отчёты доступны в CI как артефакты:
            🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}